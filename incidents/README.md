# Incidents

Keep evidence-first incident reports for deliberately injected or naturally encountered lab failures here.

Use the sequence:

```text
symptom → expected state/path → evidence → hypothesis → test → root cause → fix → validation → prevention
```

A successful repair without a defensible evidence chain is useful practice, but it is not yet a portfolio incident artifact.
